{
  "buildCommand": "pnpm install && pnpm --filter @workspace/jabeen-jewels run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "artifacts/jabeen-jewels/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!_next).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.mjs": {
      "maxDuration": 30
    }
  }
}
